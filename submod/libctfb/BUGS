#1	===========================
	ctfb_lineToArr generate out of bound (too long array in y dimension)
	added indexchecking to ctfb_drawDiagLine as a temp fix

	int ctfb_lineToArr( /* generates garbage results for 2nd octant TODO BUG 1 */
		TEMPLATE3(arr,arrStrct,ctvc2) *arr,
		ctvc2* src, 
		ctvc2* dest);
		
	int
	ctfb_drawDiagLine(
		obj_ctfb *this,
		ctvc2* src,
		ctvc2* dest,
		fwchar *ap_fwchar);
	=====================================
